<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <link rel="stylesheet" href="/fontawesome/fontawesome-free-6.3.0-web/css/all.min.css">
  <link rel="stylesheet" href="/stylesheets/reset_password.css">
  <title>Thay đổi mật khẩu</title>
</head>

<body>
  <div class="container">
    <div class="row">
      <div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
        <div class="card border-0 shadow rounded-3 my-5">
          <div class="card-body p-4 p-sm-5">
            <h5 class="card-title text-center mb-5 fw-light fs-5">THAY ĐỔI MẬT KHẨU</h5>
            <form onsubmit="return false">
              <div style="display: none;">
                <p id="user-id"><%= user_id %></p>
                <p id="token"><%= token %></p>
              </div>
              <div class="form-floating mb-3 wrap-input">
                <input type="password" class="form-control" id="password" placeholder="Nhập mật khẩu mới" required
                  maxlength="30" pattern="^(?=.*\d)(?=.*[A-Z])(?=.*[!@#&]).{8,30}$"
                  title="Mật khẩu từ 8 ký tự trở lên, có ít nhật một ký tự chữ, số và ký tự đặc biệt(!@#&)"
                  value="Hung1234@">
                <label for="password">Nhập mật khẩu mới</label>
                <i class="fa-solid fa-eye-slash eye-pass"></i>
              </div>
              <div class="form-floating mb-3 wrap-input">
                <input type="password" class="form-control" id="re-password" placeholder="Nhập lại mật khẩu mới"
                  required maxlength="30" pattern="^(?=.*\d)(?=.*[A-Z])(?=.*[!@#&]).{8,30}$"
                  title="Mật khẩu từ 8 ký tự trở lên, có ít nhật một ký tự chữ, số và ký tự đặc biệt(!@#&)"
                  value="Hung1234@">
                <label for="re-password">Nhập lại mật khẩu mới</label>
                <i class="fa-solid fa-eye-slash eye-pass"></i>
              </div>
              <div class="d-grid">
                <button class="btn btn-login text-uppercase fw-bold" id="btnComfirm" style="background-color: rgb(255, 77, 0); color: #ffffff;">XÁC NHẬN</button>
              </div>
              <p class="my-3" id="inform"></p>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.3/jquery.min.js"></script>
  <script src="/javascripts/reset_password.js"></script>
</body>

</html>